apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sandeep-pvc11
  namespace: ns11
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: manual
  selector:
    matchLabels:
      type: fast-11
