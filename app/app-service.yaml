apiVersion: v1
kind: Service
metadata:
  name: taskapp-service
spec:
  selector:
    app: taskapp
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30041
  type: NodePort